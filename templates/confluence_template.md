# Test Plan: SMTP Proxy — {FEATURE_NAME}

**Feature:** {FEATURE_NAME}
**Component:** SMTP Proxy
**Author:** {QE_OWNER}
**Date:** {DATE}
**PRD Source:** {PRD_SOURCE}
**Status:** Draft

---

## 1. Overview

{2–4 sentence summary of the feature under test, derived directly from the PRD.
Describe what the feature does, what problem it solves, and the key behaviors that
must be verified.}

---

## 2. Scope

### In Scope
{Bulleted list of feature behaviors, flows, and components covered by this test plan.
Derived strictly from the PRD — do not add anything not described in the PRD.}

- {behavior 1}
- {behavior 2}
- {behavior N}

### Out of Scope
{Behaviors, systems, or configurations explicitly excluded.
Include adjacent SMTP features not mentioned in the PRD.}

- {excluded item 1}
- {excluded item 2}

---

## 3. Test Strategy

### Test Types Applied

| Test Type | Rationale |
|---|---|
| Functional | Verify each PRD requirement produces the correct SMTP response and system behavior |
| Negative | Verify error handling, protocol violations, and invalid input rejection |
| Security | Verify feature does not introduce injection, bypass, or information-disclosure risks |
| Boundary | Verify behavior at min/max limits defined in PRD |
| Integration | Verify end-to-end flow across SMTP proxy, relay config, DLP, and downstream mail server |

### Automation Approach

| Category | Tool / Method |
|---|---|
| SMTP protocol tests | Python `smtplib` via raw socket (`server.sock.sendall`) |
| Feature flag management | `update_feature_flags` API + `get_featureConfigjson_kube` |
| Mail delivery verification | SSH + `docker exec` into mail server, read `Maildir/new` |
| Log verification | `kubectl logs -n <SMTP_PROXY_NS> <SMTP_PROXY_POD> --since=Xs` |
| Policy setup/teardown | REST API (`create_relay_config`, `create_rt_policy`, `delete_*`) |

### Test Environment Requirements

| Item | Value |
|---|---|
| SMTP Proxy VIP | `<SMTP_PROXY_VIP>` |
| SMTP Proxy Port | `<SMTP_PROXY_PORT>` |
| Kubernetes Namespace | `<SMTP_PROXY_NS>` |
| Downstream Mail Server | `<MAILSERVER_HOST>` |
| Tenant ID | `<TENANT_ID>` |

---

## 4. Coverage Matrix

{Matrix of PRD requirements × test types. Mark each cell as: ✅ covered / ➖ not applicable / ❌ not covered (explain why).}

| PRD Requirement / Feature Area | Functional | Negative | Security | Boundary | Integration |
|---|---|---|---|---|---|
| {requirement 1} | ✅ | ✅ | ➖ | ➖ | ✅ |
| {requirement 2} | ✅ | ✅ | ✅ | ✅ | ➖ |
| {requirement N} | ✅ | ➖ | ➖ | ✅ | ➖ |

**Coverage totals:**
- P0 tests: {N}
- P1 tests: {N}
- P2 tests: {N}
- Total tests: {N}
- Automatable: {N} / {Total}

---

## 5. Test Cases

> Full detail (Steps + Expected Results) is in the TestRail CSV:
> `{feature}_testrail.csv`
>
> This section provides a summary table for quick review.

| # | Test Summary | Category | Priority | Automatable |
|---|---|---|---|---|
| 1 | {test summary} | {category} | {P0–P3} | Yes / No |
| 2 | {test summary} | {category} | {P0–P3} | Yes / No |
| N | {test summary} | {category} | {P0–P3} | Yes / No |

---

## 6. Test Data

{Describe test data needed. Use placeholder variable names matching testrail_format_reference.md.}

| Data Item | Source | Notes |
|---|---|---|
| Sender domain + relay config | Created via API in test setup | Deleted in teardown |
| Recipient mailbox | `<MAILSERVER_HOST>` Maildir | Cleared before each test |
| DKIM key pair | Pre-provisioned at `<DKIM_PRIVATE_KEY>` | Selector: `<DKIM_SELECTOR>` |
| DLP policy (if needed) | Created via API | Deleted in teardown |
| Feature flags | Toggled via `update_feature_flags` API | Restored in teardown |

---

## 7. Risks and Mitigations

| Risk | Impact | Mitigation |
|---|---|---|
| {risk 1} | {High/Med/Low} | {mitigation} |
| {risk 2} | {High/Med/Low} | {mitigation} |

---

## 8. Entry / Exit Criteria

### Entry Criteria (before testing begins)
- SMTP Proxy deployed and reachable at `<SMTP_PROXY_VIP>:<SMTP_PROXY_PORT>`
- Feature flags accessible and toggleable via API
- Downstream mail server accessible via SSH at `<MAILSERVER_HOST>`
- Relay config and RT policy can be created/deleted via API

### Exit Criteria (testing complete)
- All P0 tests pass
- All P1 tests pass or have documented known issues with bug tickets
- P2/P3 failures triaged and accepted or tracked
- Test results uploaded to TestRail

---

## 9. Open Questions

{List any ambiguities from the PRD that need Dev/PM clarification before or during testing.}

- [ ] {question 1}
- [ ] {question 2}

---

*Generated by `smtp-testplan-generator` skill from PRD: {PRD_SOURCE}*
